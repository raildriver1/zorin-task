# Менеджер Автомойки "АвтомойкаПро"

Это комплексное веб-приложение, разработанное для эффективного управления всеми аспектами работы автомойки. Оно позволяет вести учет моек, управлять персоналом, клиентами и партнерами, а также автоматически рассчитывать зарплату и генерировать аналитические отчеты.

## 🚀 Ключевые возможности

*   **Панель управления (Dashboard)**: Наглядное представление ключевых метрик бизнеса в реальном времени, включая выручку, количество моек и распределение по типам оплаты.
*   **Рабочая станция**: Интеллектуальный интерфейс для быстрой регистрации новых моек. Автоматически определяет тип клиента (розничный, контрагент, агрегатор) по номеру машины и применяет соответствующие прайс-листы.
*   **Журнал моек**: Полная история всех зарегистрированных операций с возможностью поиска, редактирования и удаления записей.
*   **Управление сущностями**:
    *   **Сотрудники**: Ведение базы данных сотрудников, их контактной информации и реквизитов.
    *   **Контрагенты**: Управление корпоративными клиентами, их автопарками и индивидуальными прайс-листами.
    *   **Агрегаторы**: Управление партнерами (например, "Дорожная Сеть"), их прайс-листами и автопарками.
    *   **Настройки "Наличка"**: Централизованное управление розничными ценами для клиентов с оплатой наличными или картой.
*   **Гибкая система зарплат**:
    *   **Схемы зарплат**: Создание неограниченного количества схем двух типов: **процент от выручки** и **фиксированная ставка** за услугу.
    *   **Автоматический расчет**: Система корректно рассчитывает зарплату для каждого сотрудника, даже если в одной мойке участвуют работники с разными схемами оплаты.
    *   **Отчет по зарплате**: Детализированный отчет с общей суммой начислений и разбивкой по каждой мойке для каждого сотрудника.

## 🛠️ Технологический стек

*   **Фреймворк**: Next.js (с App Router)
*   **Язык**: TypeScript
*   **UI**: React, ShadCN UI
*   **Стилизация**: Tailwind CSS

## 📁 Структура проекта

*   **/src/app/**: Основные страницы приложения, включая панели управления, формы и списки.
*   **/src/data/**: "База данных" приложения. Вся информация (о сотрудниках, клиентах, мойках) хранится в виде JSON-файлов в соответствующих поддиректориях.
*   **/src/components/**: Переиспользуемые UI-компоненты (формы, кнопки, элементы навигации).
*   **/src/lib/**: Вспомогательные утилиты и функции загрузки данных.
*   **/src/services/**: Бизнес-логика приложения, например, калькулятор зарплат (`salary-calculator.ts`).
*   **/src/types/**: Определения типов TypeScript для всех сущностей проекта.

## ⚙️ Логика работы

### Регистрация мойки

1.  **Выбор команды**: На Рабочей станции менеджер выбирает сотрудников, которые вышли на смену.
2.  **Ввод номера машины**: После ввода госномера система автоматически проверяет его по базам контрагентов и агрегаторов.
3.  **Определение клиента**:
    *   Если номер найден в базе **контрагента**, применяется его индивидуальный прайс-лист.
    *   Если номер не найден, система предлагает выбрать тип оплаты: **наличные/карта** (применяется розничный прайс-лист) или **агрегатор** (применяется прайс-лист выбранного агрегатора).
4.  **Выбор услуг**: На основе типа клиента отображается соответствующий список услуг.
5.  **Подтверждение**: После выбора услуг и подтверждения, создается запись в **Журнале моек**, и данные становятся доступны для отчетов и расчета зарплат.

### Расчет зарплаты

Система использует сложный, но справедливый алгоритм, описанный в `src/services/salary-calculator.ts`:

*   **Доля участия**: Если над одной мойкой работает несколько человек, вознаграждение делится между ними пропорционально их количеству (по умолчанию — поровну).
*   **Схема "Процент"**: Сотрудник получает указанный процент от *своей доли* чистой выручки (за вычетом эквайринга и общих вычетов, если они есть).
*   **Схема "Ставка"**: Сотрудник получает *свою долю* от фиксированной ставки за каждую выполненную услугу, к которой он привязан через схему.

Этот подход позволяет корректно рассчитывать зарплату в смешанных командах.

## 🚀 Загрузка проекта на GitHub

Чтобы отправить все сохраненные изменения на GitHub, вам нужно пройти аутентификацию с помощью **персонального токена доступа (PAT)**. Следуйте этой инструкции предельно внимательно.

### Шаг 1: Создайте новый токен доступа (если старый не работает)

Ошибка `Invalid username or token` почти всегда означает, что ваш старый токен истек или был введен неверно. **Самый надежный способ — создать новый.**

1.  **Перейдите на страницу создания токена** по этой прямой ссылке:
    *   [**https://github.com/settings/tokens/new**](https://github.com/settings/tokens/new)

2.  **Назовите токен**: В поле **Note** напишите название, чтобы не забыть, для чего он. Например: `car-wash-pc`.

3.  **Установите срок действия**: В **Expiration** выберите `30 days` (30 дней).

4.  **Выберите права (Scopes)**: Это самый важный пункт. Поставьте **ТОЛЬКО ОДНУ** галочку напротив `repo`. Это даст токену права на работу с вашими репозиториями.
    

5.  **Сгенерируйте токен**: Пролистайте вниз и нажмите зеленую кнопку **"Generate token"**.

6.  **СКОПИРУЙТЕ И СОХРАНИТЕ ТОКЕН!**
    *   GitHub покажет вам токен только один раз. Он выглядит как `ghp_...`
    *   **Сразу же скопируйте его** и временно вставьте в любой текстовый редактор (например, Блокнот). **Не закрывайте эту страницу, пока не сохраните токен!**
    


### Шаг 2: Выполните команду `push` в терминале

Теперь, когда у вас есть новый и точно рабочий токен, выполните команду для отправки ваших изменений.

```bash
git push origin main
```

### Шаг 3: Пошаговое руководство по вводу токена

Терминал запросит у вас данные **по очереди**. Действуйте внимательно.

1.  **Сначала он запросит имя пользователя:**
    ```
    Username for 'https://github.com': 
    ```
    *   Введите сюда ваш **логин** от GitHub (в вашем случае, `danil34d`) и нажмите Enter.

2.  **Затем он запросит пароль:**
    ```
    Password for 'https://danil34d@github.com': 
    ```
    *   **ВНИМАНИЕ!** Вместо вашего обычного пароля вставьте сюда **НОВЫЙ токен**, который вы скопировали на Шаге 1.
    *   При вставке токена **символы не будут отображаться** в терминале. Это нормально. Просто вставьте и нажмите Enter.

Если все сделано правильно, ваши файлы успешно загрузятся на GitHub.

### Если ничего не помогает: решаем проблему с кэшем

Иногда операционная система может запомнить (закэшировать) старый неверный токен и не давать вам ввести новый. Если вы уверены, что вводите все правильно, но ошибка повторяется, попробуйте "сбросить" кэш.

Выполните в терминале следующую команду, **скопировав ее целиком**:

```bash
git remote set-url origin https://danil34d@github.com/danil34d/NewRepo.git
```

Эта команда обновит URL вашего репозитория, добавив в него имя пользователя. После ее выполнения снова попробуйте выполнить:

```bash
git push origin main
```

Это должно заставить Git гарантированно запросить у вас пароль (токен) заново.

### 🆘 Если ничего не помогает: Git не запрашивает пароль

Если после ввода команды `git push origin main` и вашего имени пользователя терминал **не запрашивает пароль**, а сразу выдает ошибку, это значит, что система использует старый неверный пароль из своего кэша.

Чтобы это исправить, выполните следующую команду **один раз**:
```bash
git config --global --unset credential.helper
```
Эта команда временно отключит "помощника" по учетным данным.

Сразу после этого снова попробуйте выполнить:
```bash
git push origin main
```
Теперь терминал **обязан** запросить у вас и имя пользователя, и пароль (токен).

## 🖥️ Запуск проекта на локальном компьютере

### Шаг 1: Установка Node.js

1.  Перейдите на официальный сайт: [https://nodejs.org/](https://nodejs.org/).
2.  Скачайте и установите **LTS**-версию. `npm` установится автоматически.
3.  Проверьте установку, открыв терминал и выполнив команду `node -v`.

### Шаг 2: Настройка проекта

1.  Поместите все файлы проекта в одну папку.
2.  Откройте терминал **в этой папке**.
3.  Выполните команду для установки всех зависимостей:
    ```bash
    npm install
    ```

### Шаг 3: Запуск приложения

1.  В том же терминале выполните команду:
    ```bash
    npm run dev
    ```
2.  Сервер запустится. В терминале вы увидите адрес, обычно это [http://localhost:3000](http://localhost:3000).
3.  Откройте этот адрес в вашем браузере.

Готово! Теперь приложение работает на вашем компьютере.

```