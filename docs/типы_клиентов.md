# Сводка по типам клиентов: Контрагенты, Агрегаторы и Розница

Этот документ описывает ключевые различия и особенности трех основных типов клиентов в системе "АвтомойкаПро": Контрагенты, Агрегаторы и Розничные клиенты (Наличка/Карта/Перевод).

---

## 1. Контрагенты (`counter-agents`)

**Суть:** Это постоянные корпоративные клиенты, с которыми у вас есть прямой договор. Система автоматически распознает их по номеру машины и ведет учет их задолженности.

**Ключевые особенности:**

*   **Автоматическая идентификация**: Как только менеджер вводит гос. номер машины из автопарка контрагента, система **мгновенно определяет клиента** и применяет его индивидуальные условия.
*   **Учет баланса**: У каждого контрагента есть поле `balance`. При регистрации мойки баланс **уменьшается** на сумму мойки. При поступлении оплаты (регистрируется на странице финансов клиента) баланс **увеличивается**.
*   **Фиксированный автопарк**: У каждого контрагента есть свой строго определенный список машин.
*   **Индивидуальный прайс-лист**:
    *   Имеет один основной (`priceList`) и один дополнительный (`additionalPriceList`) прайс-лист.
    *   Цены в этих прайс-листах уникальны для данного контрагента.
*   **Гибкие доп. услуги**: Вы можете настроить, разрешено ли менеджеру добавлять для этого контрагента **произвольные услуги** с кастомной ценой прямо на рабочей станции (`allowCustomServices`).
*   **Тип оплаты в системе**: `counterAgentContract`.

---

## 2. Агрегаторы (`aggregators`)

**Суть:** Это компании-партнеры (например, "Дорожная Сеть", "Шинная Лига"), которые направляют к вам на мойку своих клиентов. Система также ведет учет баланса с ними.

**Ключевые особенности:**

*   **Ручной выбор**: Менеджер **вручную выбирает** тип оплаты "Агрегатор", а затем — конкретного агрегатора из списка.
*   **Учет баланса**: Аналогично контрагентам, у агрегаторов есть поле `balance`, которое автоматически обновляется при регистрации моек и платежей.
*   **Подсказка по номеру машины**: Если номер машины найден в автопарке агрегатора, система выводит **подсказку**, но не выбирает этого клиента автоматически. Это сделано на случай, если водитель решит оплатить мойку наличными.
*   **Множественные прайс-листы**:
    *   Агрегатор может иметь **несколько именованных прайс-листов** (например, "Летний", "Зимний").
    *   Один из них всегда назначен **активным** (`activePriceListName`). Именно он используется на рабочей станции.
*   **Строгие услуги**: У агрегаторов нет понятия "дополнительного прайс-листа" или "произвольных услуг". Менеджер может выбрать услуги только из активного прайс-листа.
*   **Сохранение контекста**: При регистрации мойки в `WashEvent` сохраняется поле `priceListName` для точного расчета зарплаты по ставке в будущем.
*   **Тип оплаты в системе**: `aggregator`.

---

## 3. Наличка / Розница (`retail`)

**Суть:** Это все разовые клиенты, которые оплачивают услуги напрямую на месте — наличными, картой или переводом. Это тип клиента "по умолчанию".

**Ключевые особенности:**

*   **Ручной выбор**: Менеджер выбирает один из трех типов оплаты: "Наличные", "Карта" или "Перевод".
*   **Глобальный прайс-лист**: Все розничные клиенты обслуживаются по единому прайс-листу, который настраивается на странице **Настройки -> Прайс-лист "Наличка"**.
*   **Структура прайс-листа**: Как и у контрагентов, имеет основной (`mainPriceList`) и дополнительный (`additionalPriceList`) списки услуг.
*   **Гибкие доп. услуги**: Можно глобально разрешить или запретить менеджеру добавлять произвольные услуги для всех розничных клиентов (`allowCustomRetailServices`). Это делается на странице настроек прайс-листа.
*   **Учет эквайринга**: Только для этого типа клиентов (при оплате картой) рассчитывается и вычитается комиссия за эквайринг.
*   **Типы оплаты в системе**: `cash`, `card`, `transfer`.

---

## Сравнительная таблица

| Характеристика                | ✅ Контрагенты                                | ⚙️ Агрегаторы                                  | 💵 Наличка / Розница                                   |
| ------------------------------ | --------------------------------------------- | ---------------------------------------------- | ------------------------------------------------------ |
| **Идентификация**              | Автоматически по номеру машины                | Вручную (с подсказкой по номеру)               | Вручную                                                |
| **Учет баланса**               | Да, автоматический                            | Да, автоматический                             | Нет                                                    |
| **Структура прайс-листа**      | 1 основной + 1 дополнительный                 | Несколько именованных, один — активный         | 1 основной + 1 дополнительный (глобальный)             |
| **Произвольные доп. услуги**   | Да (настраивается для каждого)                | Нет                                            | Да (настраивается глобально)                           |
| **Связь с машиной**            | Жесткая (машина принадлежит контрагенту)     | Гибкая (машина может быть в базе)              | Нет                                                    |
| **Тип оплаты в `WashEvent`**   | `counterAgentContract`                        | `aggregator`                                   | `cash`, `card`, `transfer`                             |
| **Сохранение прайс-листа**     | Нет (всегда один)                             | Да (`priceListName`)                           | Нет                                                    |

---

## 4. Поля для заполнения (Контрагент)

Когда менеджер создает или редактирует карточку контрагента, он работает со следующими полями:

### Основная информация
*   **Имя агента**: Главное название контрагента, которое будет отображаться во всех списках.
*   **Текущий баланс**: Поле для ручной корректировки баланса. Используется для регистрации платежей от клиента.

### Связанные компании
*   **Название компании**: Официальное юридическое название.
*   **Имя представителя**: Контактное лицо.
*   **Реквизиты для счетов**: ИНН, КПП, Адрес, Банковские реквизиты и т.д.

### Автопарк
*   **Список номеров машин**: Большое текстовое поле, куда вставляются все госномера машин, принадлежащих этому контрагенту.

### Ценообразование
*   **Индивидуальный прайс-лист (основные услуги)**.
*   **Прайс-лист на доп. услуги**.
*   **Разрешить произвольные доп. услуги**: Переключатель для разрешения/запрета добавления кастомных услуг на рабочей станции.
